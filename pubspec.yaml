name: taqseet
description: "تطبيق تنظيم واتمتة عمل محلات البيع بالتقسيط الصغيرة."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  drift: ^2.28.0
  sqlite3_flutter_libs: ^0.5.34
  path_provider: ^2.1.5
  file_picker: ^10.3.10
  intl: ^0.20.2
  path: ^1.9.1
  sqlite3: ^3.4.0
  shared_preferences: ^2.5.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/branding/app_logo.png
